datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Student {
  student_id String @id @default(uuid())
  student_name String
  grade String
  blood_group String
}
