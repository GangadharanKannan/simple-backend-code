datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Hospital {
  patient_id String @id @map("_id")
  patient_name String
  disease String
  address String
}
